# My Hotel Core

<!-- Version 0.1.0+f93fd253, generated by TSpec from MyHotel.Core.Spec. Do not edit by hand. -->
`Subject under test: RoomService`

## When add room
`Return type: Room`

### Given no such room
```
IRoomStore.Load() returns zero Rooms
When Add(a Room)
```

- **return the room** — `Result is the Room`
- **store it** — `IRoomStore.Save(one Room)`

### Given the room number is taken
```
IRoomStore.Load() returns [the Room with { BedCount = any int }]
When Add(a Room)
```

- **throw room already exists** — `throws RoomAlreadyExists`

## When delete room

### Given no such room
```
IRoomStore.Load() returns zero Rooms
When Delete(a Room's RoomNumber)
```

- **throw not found** — `throws RoomNotFound`

### Given the room is stored
```
IRoomStore.Load() returns one Room
When Delete(a Room's RoomNumber)
```

- **store what is left** — `IRoomStore.Save(zero Rooms)`

## When get room
`Return type: Room`

### Given no such room
```
IRoomStore.Load() returns zero Rooms
When Get(a Room's RoomNumber)
```

- **throw not found** — `throws RoomNotFound`

### Given the room is stored
```
IRoomStore.Load() returns one Room
When Get(a Room's RoomNumber)
```

- **return it** — `Result is the Room`

## When list rooms
`Return type: IReadOnlyList<Room>`

### Given no rooms
```
IRoomStore.Load() returns zero Rooms
When List()
```

- **return no rooms** — `Result is empty`

### Given two rooms
```
IRoomStore.Load() returns two Rooms
When List()
```

- **return them in the order they are stored** — `Result is equal to two Rooms`

## When update room
`Return type: Room`

### Given another room is stored first
```
IRoomStore.Load() returns [a third Room, the Room]
When Update(a Room's RoomNumber, a second Room)
```

- **keep it where it was** — `IRoomStore.Save(new[] { the third Room, the second Room })`

### Given no such room
```
IRoomStore.Load() returns zero Rooms
When Update(a Room's RoomNumber, a second Room)
```

- **throw not found** — `throws RoomNotFound`

### Given the room is stored
```
IRoomStore.Load() returns one Room
When Update(a Room's RoomNumber, a second Room)
```

- **return the new room** — `Result is a second Room`
