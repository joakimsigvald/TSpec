# My Hotel

<!-- Version 0.1.0+2af04ec0, generated by TSpec from MyHotel.Spec. Do not edit by hand. -->
```
Subject under test: Hotel
Return type:        HttpResponseMessage
```

## When get version
`Api.GetAsync("/version")`

- **respond ok** — `Result.StatusCode is OK`
- **return the application version** — `Result.Read<VersionInfo>().Version is DeclaredVersion`

## When add room
`Api.PostAsJsonAsync("/rooms", a Room)`

### Given no such room

- **respond created** — `Result.StatusCode is Created`
- **return the room** — `Result.Read<Room>() is the Room`
- **point at the new room** — `Result.Headers.Location.ToString() is "/rooms/{the Room's RoomNumber}"`

### Given the room already exists
`Having Api.PostAsJsonAsync("/rooms", the Room with { BedCount = any int })`

- **respond conflict** — `Result.StatusCode is Conflict`

## When delete room
`Api.DeleteAsync("/rooms/{a Room's RoomNumber}")`

### Given no such room

- **respond not found** — `Result.StatusCode is NotFound`
- **say which room** — `Result.Read<ErrorBody>().Error contains the Room's RoomNumber`

### Given the room exists
`Having Api.PostAsJsonAsync("/rooms", the Room)`

- **respond no content** — `Result.StatusCode is NoContent`

## When update room
```
Api.PutAsJsonAsync("/rooms/{a Room's RoomNumber}", the Room with {
      BedCount = a second int })
```

### Given no such room

- **respond not found** — `Result.StatusCode is NotFound`
- **say which room** — `Result.Read<ErrorBody>().Error contains the Room's RoomNumber`

### Given the room exists
`Having Api.PostAsJsonAsync("/rooms", the Room)`

- **respond ok** — `Result.StatusCode is OK`
- **return the updated room** — `Result.Read<Room>() is the Room with { BedCount = a second int }`

## When list rooms
`Api.GetAsync("/rooms")`

### Given no rooms

- **respond ok** — `Result.StatusCode is OK`
- **return no rooms** — `Result.Read<Room[]>() is empty`

### Given two rooms
```
Having Api.PostAsJsonAsync("/rooms", a second Room)
  after Api.PostAsJsonAsync("/rooms", a Room)
```

- **respond ok** — `Result.StatusCode is OK`
- **return both rooms in the order they were created** — `Result.Read<Room[]>() is equal to two Rooms`

## When get room
`Api.GetAsync("/rooms/{a Room's RoomNumber}")`

### Given no such room

- **respond not found** — `Result.StatusCode is NotFound`
- **say which room** — `Result.Read<ErrorBody>().Error contains the Room's RoomNumber`

### Given the room exists
`Having Api.PostAsJsonAsync("/rooms", the Room)`

- **respond ok** — `Result.StatusCode is OK`
- **return the room** — `Result.Read<Room>() is the Room`

### Given the application was restarted
```
Having Restart()
  after Api.PostAsJsonAsync("/rooms", the Room)
```

- **respond ok** — `Result.StatusCode is OK`
- **return the room** — `Result.Read<Room>() is the Room`

### Given the room was deleted
```
Having Api.DeleteAsync("/rooms/{the Room's RoomNumber}")
  after Api.PostAsJsonAsync("/rooms", the Room)
```

- **respond not found** — `Result.StatusCode is NotFound`
- **say which room** — `Result.Read<ErrorBody>().Error contains the Room's RoomNumber`

### Given the room was updated
```
Having Api.PutAsJsonAsync("/rooms/{the Room's RoomNumber}", the Room with {
      BedCount = a second int })
  after Api.PostAsJsonAsync("/rooms", the Room)
```

- **return the updated room** — `Result.Read<Room>() is the Room with { BedCount = a second int }`
