# MyHotel

Version 0.1.0, hash 2c5dfbdf

<!-- Generated by TSpec from MyHotel.Spec. Do not edit by hand. -->

## WhenAddRoom

### GivenNoSuchRoom.ThenPointAtTheNewRoom

```
Using owned api
  and owned api.CreateClient
When api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Then Result.Headers.Location.ToString() is "/rooms/{RoomNumber}"
```

### GivenNoSuchRoom.ThenRespondCreated

```
Using owned api
  and owned api.CreateClient
When api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Then Result.StatusCode is HttpStatusCode.Created
```

### GivenNoSuchRoom.ThenReturnTheRoom

```
Using owned api
  and owned api.CreateClient
When api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Then Result.Read<Room>() is new Room(RoomNumber, 2)
```

### GivenTheRoomAlreadyExists.ThenRespondConflict

```
Using owned api
  and owned api.CreateClient
When api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Having api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 3))
Then Result.StatusCode is HttpStatusCode.Conflict
```

## WhenGetRoom

### GivenNoSuchRoom.ThenRespondNotFound

```
Using owned api
  and owned api.CreateClient
When api.GetAsync("/rooms/{RoomNumber}")
Then Result.StatusCode is HttpStatusCode.NotFound
```

### GivenTheRoomExists.ThenRespondOk

```
Using owned api
  and owned api.CreateClient
When api.GetAsync("/rooms/{RoomNumber}")
Having api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Then Result.StatusCode is HttpStatusCode.OK
```

### GivenTheRoomExists.ThenReturnTheRoom

```
Using owned api
  and owned api.CreateClient
When api.GetAsync("/rooms/{RoomNumber}")
Having api.PostAsJsonAsync("/rooms", new Room(RoomNumber, 2))
Then Result.Read<Room>() is new Room(RoomNumber, 2)
```

## WhenGetVersion

### ThenRespondOk

```
Using owned api
  and owned api.CreateClient
When api.GetAsync("/version")
Then Result.StatusCode is HttpStatusCode.OK
```

### ThenReturnTheApplicationVersion

```
Using owned api
  and owned api.CreateClient
When api.GetAsync("/version")
Then Result.Read<VersionInfo>().Version is DeclaredVersion
```
