# My Hotel

<!-- Version 0.1.0+934967ef, generated by TSpec from MyHotel.Spec. Do not edit by hand. -->
```
Subject under test: HttpClient
Return type:        HttpResponseMessage

Using owned api
  and owned api.CreateClient
```

## When get version
`api.GetAsync("/version")`

- **respond ok** — `Result.StatusCode is OK`
- **return the application version** — `Result.Read<VersionInfo>().Version is DeclaredVersion`

## When add room
`api.PostAsJsonAsync("/rooms", new Room(the RoomNumber, an int))`

### Given no such room

- **respond created** — `Result.StatusCode is Created`
- **return the room** — `Result.Read<Room>() is new Room(the RoomNumber, the int)`
- **point at the new room** — `Result.Headers.Location.ToString() is "/rooms/{the RoomNumber}"`

### Given the room already exists
`Having api.PostAsJsonAsync("/rooms", new Room(the RoomNumber, any int))`

- **respond conflict** — `Result.StatusCode is Conflict`

## When get room
`api.GetAsync("/rooms/{the RoomNumber}")`

### Given no such room

- **respond not found** — `Result.StatusCode is NotFound`

### Given the room exists
`Having api.PostAsJsonAsync("/rooms", new Room(the RoomNumber, an int))`

- **respond ok** — `Result.StatusCode is OK`
- **return the room** — `Result.Read<Room>() is new Room(the RoomNumber, the int)`

## When list rooms
`api.GetAsync("/rooms")`

### Given no rooms

- **respond ok** — `Result.StatusCode is OK`
- **return no rooms** — `Result.Read<Room[]>() is empty`

### Given two rooms
```
Having api.PostAsJsonAsync("/rooms", new Room(a second string, a second int))
  and api.PostAsJsonAsync("/rooms", new Room(a string, an int))
```

- **respond ok** — `Result.StatusCode is OK`
- **return both rooms in the order they were created**
  ```
  Result.Read<Room[]>() has two items that first is
        new Room(the string, the int)
  Second is new Room(the second string, the second int)
  ```
